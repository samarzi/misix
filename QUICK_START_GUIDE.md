# 🚀 Быстрый старт MISIX

## Telegram бот

### 1. Запусти бота:
```bash
cd backend
source venv/bin/activate  # или .venv/bin/activate
python test_bot_quick.py
```

### 2. Протестируй в Telegram:
- Отправь `/start` - увидишь кнопки
- Нажми "❓ Помощь" - покажет справку
- Нажми "🛌 Я спать" - начнёт трекинг сна
- Отправь голосовое "напомни купить молоко" - распознает и создаст задачу
- Отправь текст "потратил 500₽ на кофе" - создаст расход

### 3. Команды:
- `/help` - справка
- `/tasks` - список задач
- `/finances` - финансовая сводка
- `/mood` - история настроения
- `/sleep` - начать трекинг сна
- `/wake` - закончить трекинг сна

---

## Веб-приложение

### 1. Запусти backend API (в одном терминале):
```bash
cd backend
source venv/bin/activate
uvicorn app.web.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. Запусти frontend (в другом терминале):
```bash
cd frontend
npm run dev
```

### 3. Открой в браузере:
```
http://localhost:5173
```

---

## Что исправлено

✅ **Голосовые сообщения** - теперь распознаются и обрабатываются  
✅ **Кнопки** - "Помощь" и "Я спать" работают  
✅ **Команды** - все 7 команд работают  
✅ **Frontend** - настроен .env.local  

---

## Если что-то не работает

### Бот не отвечает:
```bash
cd backend
python diagnose.py
```

### Голосовые не работают:
Проверь в `.env`:
```
YANDEX_GPT_API_KEY=...
YANDEX_FOLDER_ID=...
```

### Frontend не подключается:
Проверь что backend API запущен на порту 8000:
```bash
curl http://localhost:8000/health
```

---

**Готово! Всё работает!** 🎉
