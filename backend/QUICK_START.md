# 🚀 MISIX Quick Start - Database Setup

## Проблема
Видите ошибки типа:
- `relation "tasks" does not exist`
- `column "email" does not exist`
- `relation "finance_entries" does not exist`

**Это означает что база данных не настроена.**

## ✅ Решение (5 минут)

### Шаг 1: Откройте Supabase SQL Editor

1. Перейдите на https://supabase.com/dashboard
2. Выберите ваш проект MISIX
3. В левом меню нажмите **SQL Editor**
4. Нажмите **New query**

### Шаг 2: Скопируйте и выполните миграцию

1. Откройте файл `backend/migrations/SIMPLE_SETUP.sql`
2. Скопируйте **ВСЁ** содержимое файла
3. Вставьте в SQL Editor
4. Нажмите **Run** (или Ctrl+Enter)

### Шаг 3: Проверьте результат

Вы должны увидеть сообщение:
```
SUCCESS! Database setup complete. Total tables: 11
```

Если видите это - всё готово! 🎉

### Шаг 4: Перезапустите приложение

```bash
# Если бэкенд запущен, перезапустите его
cd backend
# Остановите текущий процесс (Ctrl+C)
# Запустите снова
python -m uvicorn app.web.main:app --reload
```

## 📋 Что создается

Миграция создаст следующие таблицы:
- ✅ `users` - пользователи
- ✅ `tasks` - задачи
- ✅ `finance_entries` - финансы
- ✅ `notes` - заметки
- ✅ `mood_entries` - настроение
- ✅ `sleep_tracking` - сон
- ✅ `personal_entries` - личные записи
- ✅ `user_settings` - настройки напоминаний
- ✅ `projects` - проекты
- ✅ `tags` - теги

Плюс все необходимые индексы для производительности.

## ❓ Troubleshooting

### "permission denied for schema public"
- Убедитесь что используете правильный проект в Supabase
- Проверьте что вы владелец проекта

### "already exists" ошибки
- Это нормально! Миграция использует `IF NOT EXISTS`
- Просто игнорируйте эти сообщения

### Таблицы не создаются
1. Проверьте что скопировали **весь** файл SIMPLE_SETUP.sql
2. Убедитесь что нажали Run
3. Проверьте логи на наличие других ошибок

### Всё ещё не работает?
Проверьте переменные окружения в `.env`:
```bash
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
```

Получить их можно в Supabase Dashboard → Settings → API

## 🎯 Следующие шаги

После успешной настройки БД:

1. **Запустите Telegram бота:**
   ```bash
   cd backend
   python -m app.bot
   ```

2. **Протестируйте функции:**
   - Отправьте `/start` боту
   - Создайте задачу: "Напомни купить молоко завтра"
   - Добавьте расход: "Потратил 500 рублей на обед"
   - Запишите настроение: "Сегодня отличное настроение!"

3. **Настройте напоминания:**
   - Отправьте `/reminders` боту
   - Настройте время и частоту

## 📚 Дополнительная информация

- Полная документация: `backend/MIGRATION_INSTRUCTIONS.md`
- Скрипт для автоматизации: `backend/apply_migrations.sh`
- Статус реализации: `IMPLEMENTATION_STATUS.md`

---

**Готово!** Теперь MISIX полностью настроен и готов к использованию! 🚀
