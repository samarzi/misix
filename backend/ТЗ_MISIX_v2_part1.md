# 🧠 Техническое задание v2.0
## Проект: M I S I X — Интеллектуальный личный помощник

**Дата обновления:** 17 ноября 2025  
**Версия:** 2.0  
**Статус:** В разработке (65% готовности)

---

## 1. Общая концепция проекта

**M I S I X** — это интеллектуальный личный помощник нового поколения, который понимает естественный язык и автоматически структурирует информацию из разговора. Система объединяет функции психолога, планировщика, финансового помощника и друга.

**Ключевые отличия v2.0:**
- ✅ Автоматическое извлечение данных из естественного разговора
- ✅ Множественные намерения в одном сообщении
- ✅ Контекстная память с AI (Yandex GPT)
- ✅ Модульная архитектура с сервисами
- ✅ Отслеживание настроения и эмоций

---

## 2. Реализованный функционал

### 💬 Естественное общение (✅ Реализовано)

**Возможности:**
- Общение без команд - бот понимает естественный язык
- Автоматическая классификация намерений через AI
- Поддержка множественных намерений в одном сообщении
- Контекстная память (последние 6 сообщений)
- Интеграция с Yandex GPT для интеллектуальных ответов

**Примеры:**
```
Пользователь: "потратил 200₽ на такси и напомни купить молоко"
Бот: 💸 Записал расход: 200₽ (транспорт)
     ✅ Создал задачу: купить молоко
     Хорошо, записал расход на такси и добавил напоминание!
```

### 📋 Модуль "Задачи" (✅ Частично реализовано)

**Реализовано:**
- ✅ Автоматическое создание задач из текста
- ✅ Извлечение дедлайнов ("завтра", "через 2 дня")
- ✅ Определение приоритета (low/medium/high)
- ✅ Сохранение в базу данных
- ✅ API для веб-приложения

**Примеры:**
```
"напомни завтра в 9 позвонить партнеру"
"срочно купить молоко"
"через неделю встреча с клиентом"
```

**Не реализовано:**
- ❌ Напоминания (push-уведомления)
- ❌ Повторяющиеся задачи
- ❌ Подзадачи
- ❌ Теги и категории

### 💰 Модуль "Финансы" (✅ Частично реализовано)

**Реализовано:**
- ✅ Автоматический учет расходов и доходов
- ✅ Автоматическая категоризация через AI
- ✅ Поддержка различных форматов ("500₽", "500 рублей")
- ✅ Сохранение в базу данных
- ✅ API для веб-приложения

**Категории:**
- Еда и напитки
- Транспорт
- Развлечения
- Здоровье
- Покупки
- Другое

**Примеры:**
```
"потратил 500 рублей на кофе" → Расход: 500₽ (еда и напитки)
"заработал 50000" → Доход: 50000₽
"200₽ на такси" → Расход: 200₽ (транспорт)
```

**Не реализовано:**
- ❌ Отчеты и статистика
- ❌ Бюджеты и лимиты
- ❌ Графики расходов
- ❌ Экспорт данных

### 📝 Модуль "Заметки" (✅ Реализовано)

**Реализовано:**
- ✅ Автоматическое сохранение заметок
- ✅ Извлечение заголовка и содержания
- ✅ Сохранение в базу данных
- ✅ API для веб-приложения

**Примеры:**
```
"запомни что встреча в офисе на Ленина 5"
"сохрани пароль от wifi: qwerty123"
```

**Не реализовано:**
- ❌ Папки и категории
- ❌ Теги
- ❌ Поиск по заметкам
- ❌ Шифрование чувствительных данных

### 😊 Модуль "Настроение" (✅ Новое! Реализовано)

**Реализовано:**
- ✅ Автоматическое определение настроения из текста
- ✅ Оценка интенсивности (1-10)
- ✅ Поддержка различных эмоций
- ✅ Сохранение истории
- ✅ Анализ трендов

**Поддерживаемые настроения:**
- happy (счастливый)
- sad (грустный)
- anxious (тревожный)
- calm (спокойный)
- excited (возбужденный)
- tired (уставший)
- stressed (в стрессе)
- angry (злой)
- neutral (нейтральный)

**Примеры:**
```
"сегодня отличное настроение!" → 😊 Хорошее настроение (9/10)
"устал очень" → 😴 Усталость (7/10)
"немного тревожно" → 😰 Тревожное состояние (5/10)
```

---

## 3. Архитектура системы

### Технологический стек

**Backend:**
- Python 3.13
- FastAPI (веб-сервер)
- aiogram / python-telegram-bot (Telegram бот)
- Supabase (PostgreSQL база данных)
- Yandex GPT (AI для понимания языка)
- Yandex SpeechKit (распознавание голоса)

**Frontend:**
- React / Next.js
- TypeScript
- Tailwind CSS
- Deployed on Netlify

**Infrastructure:**
- Backend: Render.com
- Database: Supabase (PostgreSQL)
- Frontend: Netlify
- Version Control: GitHub

### Модульная архитектура

```
backend/
├── app/
│   ├── api/              # REST API endpoints
│   ├── bot/              # Telegram bot handlers
│   │   ├── handlers/     # Message, command, voice handlers
│   │   ├── intent_processor.py  # Intent processing
│   │   └── response_builder.py  # Response formatting
│   ├── core/             # Core utilities
│   ├── middleware/       # Logging, rate limiting, errors
│   ├── models/           # Pydantic models
│   ├── repositories/     # Database access layer
│   ├── services/         # Business logic
│   │   ├── ai_service.py           # Yandex GPT integration
│   │   ├── extraction_service.py   # Data extraction
│   │   ├── conversation_service.py # Chat history
│   │   ├── task_service.py
│   │   ├── finance_service.py
│   │   ├── note_service.py
│   │   └── mood_service.py
│   └── web/              # FastAPI app
└── migrations/           # Database migrations
```

### Поток обработки сообщения

```
1. User Message (Telegram)
   ↓
2. MessageHandler
   ↓
3. Get/Create User (UserRepository)
   ↓
4. Get Conversation Context (ConversationService)
   ↓
5. Save User Message
   ↓
6. Classify Intents (AIService + Yandex GPT)
   ↓
7. Process Each Intent (IntentProcessor)
   ├─→ Extract Task Data → TaskService → Database
   ├─→ Extract Finance Data → FinanceService → Database
   ├─→ Extract Note Data → NoteService → Database
   └─→ Extract Mood Data → MoodService → Database
   ↓
8. Build Confirmations (ResponseBuilder)
   ↓
9. Generate AI Response (AIService + context + confirmations)
   ↓
10. Save Assistant Message
   ↓
11. Send Reply to User
```

---

## 4. База данных (Supabase PostgreSQL)

### Реализованные таблицы

**users** - Пользователи
- id (UUID, PK)
- email, password_hash
- telegram_id (unique)
- username, first_name, last_name
- created_at, updated_at

**tasks** - Задачи
- id (UUID, PK)
- user_id (FK → users)
- title, description
- deadline, priority, status
- created_at, updated_at

**finance_records** - Финансы
- id (UUID, PK)
- user_id (FK → users)
- amount, type (expense/income)
- category, description
- date, created_at

**notes** - Заметки
- id (UUID, PK)
- user_id (FK → users)
- title, content
- folder_id (FK → note_folders)
- created_at, updated_at

**mood_entries** - Настроение (✅ Новое!)
- id (UUID, PK)
- user_id (FK → users)
- mood, intensity (1-10)
- note
- created_at, updated_at

**assistant_messages** - История разговоров
- id (UUID, PK)
- user_id (FK → users)
- role (user/assistant)
- content
- telegram_id
- created_at

---

## 5. AI и обработка естественного языка

### Yandex GPT Integration

**Используется для:**
1. **Классификация намерений** - определение что хочет пользователь
2. **Извлечение данных** - парсинг структурированной информации
3. **Генерация ответов** - естественные диалоги
4. **Анализ настроения** - определение эмоций

**Промпты оптимизированы для:**
- Русского языка
- Множественных намерений
- Низкой латентности (temperature=0.3)
- JSON ответов

### Confidence Threshold

Система использует порог уверенности **0.7** (70%):
- Если confidence ≥ 0.7 → создаем сущность
- Если confidence < 0.7 → продолжаем обычный разговор

Это предотвращает ложные срабатывания.

---

_Продолжение в части 2..._
